<!DOCTYPE html>
<html lang="pt-br">
<head>
    <%- include('../partials/header') %>
    <title>Perfil de <%= profileUser.username %> - CardHub</title>
    <style>
        .profile-average-rating {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-top: 1rem;
            gap: 0.5rem;
        }
        .profile-average-rating .rating-value {
            font-size: 1.2rem;
            font-weight: 600;
        }
        .profile-average-rating .star-rating .star {
            font-size: 1.5rem;
            color: #ccc;
        }
        .profile-average-rating .star-rating .star.filled {
            color: #ffc107;
        }
        .profile-average-rating .star-rating .star.half-filled {
            color: #ffc107;
            position: relative;
        }
        .profile-average-rating .star-rating .star.half-filled::before {
            content: '☆';
            position: absolute;
            left: 0;
            top: 0;
            width: 50%;
            overflow: hidden;
            color: #ffc107;
        }
        .profile-average-rating .review-count {
            font-size: 0.9rem;
            color: var(--text-muted);
        }
        .user-reviews {
            margin-top: 1.5rem;
        }
        .reviews-list {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }
        .review-item {
            border: 1px solid var(--border);
            border-radius: 8px;
            padding: 1rem;
        }
        .review-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 0.5rem;
        }
        .reviewer-name {
            font-weight: 600;
        }
        .review-rating .star {
            color: #ffc107;
        }
        .review-rating .star.filled {
            color: #ffc107;
        }
        .review-comment {
            margin-bottom: 0.5rem;
        }
        .review-date {
            font-size: 0.85rem;
            color: var(--text-muted);
            text-align: right;
        }

        /* Adiciona espaçamento na tabela de anúncios do perfil */
        .user-listings .table th {
            padding-left: 15px;
            padding-right: 15px;
            text-align: center;
        }
        .user-listings .table tbody td {
            padding: 12px 15px; /* 12px vertical, 15px horizontal */
            vertical-align: middle;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <%- include('../partials/navbar') %>
    </div>
    <main class="container profile-grid">
        <aside class="profile-sidebar">
            <div class="profile-card">
                <img src="/images/default-avatar.png" alt="Avatar de <%= profileUser.username %>" class="profile-avatar">
                <h2 class="profile-username"><%= profileUser.username %></h2>
                <div class="profile-average-rating">
                    <span class="rating-value"><%= averageRating.toFixed(1) %></span>
                    <span class="star-rating">
                        <% for(let i = 1; i <= 5; i++) { %>
                            <% if (i <= averageRating) { %>
                                <span class="star filled">★</span>
                            <% } else if (i - 0.5 <= averageRating) { %>
                                <span class="star half-filled">★</span>
                            <% } else { %>
                                <span class="star">☆</span>
                            <% } %>
                        <% } %>
                    </span>
                    <span class="review-count">(<%= reviews.length %> avaliações)</span>
                </div>
                <p class="profile-account-type"><%= profileUser.accountType === 'shop' ? 'Loja' : 'Pessoa Física' %></p>
                <% if (profileUser.accountType === 'shop' && profileUser.businessName) { %>
                    <p class="profile-business-name"><strong><%= profileUser.businessName %></strong></p>
                <% } %>
                <p class="profile-member-since">Membro desde <%= profileUser.createdAt.toLocaleDateString('pt-BR') %></p>
                <% if (user && user.id === profileUser.id.toString()) { %>
                    <% if (user.accountType === 'admin') { %>
                        <a href="/admin/dashboard" class="btn btn-primary" style="margin-top: 1rem; width: 100%;">Admin Dashboard</a>
                    <% } else { %>
                        <a href="/dashboard-vendedor" class="btn btn-primary" style="margin-top: 1rem; width: 100%;">Acessar Dashboard</a>
                    <% } %>
                <% } %>
            </div>
        </aside>

        <section class="profile-main">
            <% if (user && user.id === profileUser.id.toString()) { %>
            <div class="address-section card">
                <h2>Meu Endereço</h2>
                <p>Este endereço será usado como origem para o cálculo de fretes.</p>

                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="alert alert-danger">
                        <%= error %>
                    </div>
                <% } %>

                <form action="/profile/update" method="post" class="address-form">
                    <div class="form-group">
                        <label>CEP</label>
                        <input class="form-control" name="cep" placeholder="00000-000" value="<%= profileUser.address?.cep || '' %>" required>
                    </div>
                    <div class="form-group">
                        <label>Rua / Avenida</label>
                        <input class="form-control" name="street" placeholder="Sua rua ou avenida" value="<%= profileUser.address?.street || '' %>" required>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label>Número</label>
                            <input class="form-control" name="number" value="<%= profileUser.address?.number || '' %>" required>
                        </div>
                        <div class="form-group">
                            <label>Complemento (opcional)</label>
                            <input class="form-control" name="complement" value="<%= profileUser.address?.complement || '' %>">
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group city-group">
                            <label>Cidade</label>
                            <input class="form-control" name="city" value="<%= profileUser.address?.city || '' %>" required>
                        </div>
                        <div class="form-group state-group">
                            <label>UF</label>
                            <input class="form-control" name="state" maxlength="2" value="<%= profileUser.address?.state || '' %>" required>
                        </div>
                    </div>
                    <button class="btn btn-primary" type="submit">Salvar Endereço</button>
                </form>
            </div>
            <% } %>

            <div class="user-listings card">
                <h2>Anúncios de <%= profileUser.username %></h2>
                <% if (listings && listings.length > 0) { %>
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Imagem</th>
                                <th>Carta</th>
                                <th>Condição</th>
                                <th>Idioma</th>
                                <th>Preço</th>
                                <th>Quantidade</th>
                                <% if (user && user.id === profileUser.id.toString()) { %>
                                    <th>Ações</th>
                                <% } %>
                            </tr>
                        </thead>
                        <tbody>
                            <% listings.forEach(listing => { %>
                                <tr>
                                    <td>
                                        <% if (listing.card.image_url) { %>
                                            <img src="<%= listing.card.image_url %>" alt="<%= listing.card.name %>" style="width: 50px; border-radius: 4px;">
                                        <% } %>
                                    </td>
                                    <td><%= listing.card.name %></td>
                                    <td><%= listing.condition %></td>
                                    <td><%= listing.language %></td>
                                    <td>
                                        <% 
                                            let priceStr = 'R$ ' + Number(listing.price || 0).toFixed(2).replace('.', ',');
                                            if (priceStr.endsWith(',00')) {
                                                priceStr = priceStr.slice(0, -3);
                                            }
                                        %>
                                        <%= priceStr %>
                                    </td>
                                    <td><%= listing.quantity %></td>
                                    <% if (user && user.id === profileUser.id.toString()) { %>
                                        <td>
                                            <a href="/listings/<%= listing._id %>/edit" class="btn btn-sm btn-secondary">Editar</a>
                                            <form action="/listings/<%= listing._id %>?_method=DELETE" method="POST" style="display: inline;">
                                                <button type="submit" class="btn btn-sm btn-danger">Excluir</button>
                                            </form>
                                        </td>
                                    <% } %>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                    <div style="text-align: right; margin-top: 1rem;">
                        <a href="/meus-anuncios" class="btn btn-secondary">Ver todos os anúncios</a>
                    </div>
                <% } else {
                    %>
                    <p>Este usuário ainda não tem nenhum anúncio.</p>
                <% } %>
            </div>

            <div class="user-reviews card">
                <h2>Avaliações de <%= profileUser.username %></h2>
                <% if (reviews && reviews.length > 0) { %>
                    <div class="reviews-list">
                        <% reviews.forEach(review => { %>
                            <div class="review-item">
                                <div class="review-header">
                                    <span class="reviewer-name"><%= review.buyer.username %></span>
                                    <span class="review-rating">
                                        <% for(let i = 0; i < 5; i++) { %>
                                            <% if (i < review.rating) { %>
                                                <span class="star filled">★</span>
                                            <% } else { %>
                                                <span class="star">☆</span>
                                            <% } %>
                                        <% } %>
                                    </span>
                                </div>
                                <p class="review-comment"><%= review.comment %></p>
                                <p class="review-date"><%= new Date(review.createdAt).toLocaleDateString('pt-BR') %></p>
                            </div>
                        <% }) %>
                    </div>
                <% } else { %>
                    <p>Este usuário ainda não recebeu nenhuma avaliação.</p>
                <% } %>
            </div>
        </section>
    </main>
    <div class="container">
        <%- include('../partials/footer') %>
    </div>
    <script src="/js/main.js"></script>
</body>
</html>