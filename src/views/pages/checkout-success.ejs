<!-- src/views/pages/checkout-success.ejs -->
<%- include('../partials/header') %>
<main class="container">
  <%- include('../partials/navbar') %>

  <section class="panel" style="max-width:760px;margin:24px auto;padding:18px;">
    <h1>Pedido recebido!</h1>
    <p>Recebemos seu pedido e já estamos processando.</p>

    <div style="margin-top:12px;">
      <div style="display:flex; justify-content:space-between; margin: 6px 0;">
        <span>Subtotal</span><strong>R$ <%= totals.subtotal.toFixed(2) %></strong>
      </div>
      <div style="display:flex; justify-content:space-between; margin: 6px 0;">
        <span>Frete</span><strong>R$ <%= totals.shipping.toFixed(2) %></strong>
      </div>
      <div style="display:flex; justify-content:space-between; margin: 6px 0; font-size:1.2rem;">
        <span>Total</span><strong>R$ <%= totals.grand.toFixed(2) %></strong>
      </div>
    </div>

    <a class="btn btn-primary" href="/orders">Ver meus pedidos</a>
  </section>
</main>
<%- include('../partials/footer') %>
